ZED PROXY — کد بازیابی رمز عبور

کد بازیابی شما: {{ $code }}

این کد تا {{ $ttlMinutes }} دقیقه دیگر معتبر است و فقط یک بار قابل استفاده است.

اگر شما درخواست بازیابی رمز عبور نداده‌اید، این پیام را نادیده بگیرید — رمز عبور شما بدون این کد تغییر نمی‌کند.

این کد را با هیچ‌کس به اشتراک نگذارید؛ تیم پشتیبانی هرگز آن را از شما نمی‌پرسد.
